<% if params[:column_full] %>
<div class="column is-full">
<% else %>
<div class="column is-one-third">
<% end %>
  <div class="card">
    <div class="card-content">
      <% unless params[:no_link] %>
        <p class="title"><%= link_to foodgroup.name, foodgroup %></p>
      <% else %>
        <p class="title"><%= foodgroup.name %> </p>
      <% end %>
    </div>
    <footer class="card-footer">
      <p class="card-footer-item">
        <span class="has-text-weight-semibold">Suggested Daily Serving: <%= foodgroup.suggested_serving %></span>
      </p>
    </footer>
  </div>
</div>
<% if params[:column_full] %>
<div class="column is-full">
<% else %>
<div class="column is-one-third">
<% end %>
  <div class="card">
    <div class="card-content">
      <% unless params[:no_link] %>
        <p class="title"><%= link_to ingredient.name, ingredient %></p>
      <% else %>
        <p class="title"><%= ingredient.name %> </p>
      <% end %>
    </div>
    <footer class="card-footer">
      <p class="card-footer-item">
       <span class="has-text-weight-semibold">Description goes here<%= ingredient.description %></span>
      </p>
    </footer>
  </div>
</div>
<% if @recipes.empty? %>
 <p>There are no recipes available to preview.</p>
<% else %>
  <% @recipes.each do |recipe| %>
    <h2><%= link_to recipe.name, recipe %></h2>
    <p><%= recipe.description %></p>
    <p>This recipe is found in <%= recipe.menus.count %> menus.</p>
  <% end %>
<% end %>